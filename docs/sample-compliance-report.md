# Compliance report

Generated by `python3 scripts/compliance_report.py --db /tmp/pii.duckdb`.

Schema fingerprint `1501a19ca3d8`, taxonomy `4e7ea0d6ccd8`.

**25 of 58 questions answered. 33 refused, and 31 of those close when somebody does something.**

A refused question is not a gap in the run. It is the tool declining to publish a number about a set nobody has agreed to. Each one below carries what would close it, or says that nothing does.

## What was scanned

Recovered from the catalog rather than read from the schema file. No value from a user column was selected at any point.

```
analytics.encounter_daily      5 columns,  2 in a personal category
raw.claim                      8 columns,  3 in a personal category
raw.device_reading             7 columns,  3 in a personal category
raw.encounter                  9 columns,  5 in a personal category
raw.patient                   13 columns, 12 in a personal category
```

## What the classifier found

Three bands. Accept is masked with nobody looking, review is a person's decision, ignore is the tool saying no rule fired.

```
accept       15
ignore       17
review       10
```

Refused, 10 of them:

- `analytics.encounter_daily.day` is this column personal data? it scored 0.4500, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.claim.member_number` is this column personal data? it scored 0.7000, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.claim.payer_name` is this column personal data? it scored 0.4000, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.claim.submitted_on` is this column personal data? it scored 0.7250, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.device_reading.taken_at` is this column personal data? it scored 0.7250, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.encounter.admitted_at` is this column personal data? it scored 0.7250, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.encounter.attending_npi` is this column personal data? it scored 0.5256, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.encounter.discharged_at` is this column personal data? it scored 0.7250, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.patient.city` is this column personal data? it scored 0.6000, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.
- `raw.patient.created_at` is this column personal data? it scored 0.7250, which is inside the band where the tool declined to decide. Closes when a reviewer answers it in the queue.

## The policy that follows

Generated from the classification. A review action is not a policy, it is the absence of one, and it is counted here so the two are not confused.

```
redact       9
generalise   3
retain       20
review       10
17 of the 20 retained columns are kept because no rule fired
```

## What risk is left

k is the smallest group on the masked quasi set. k of 1 means somebody is alone in their group and the masking did not protect them.

```
nothing this section can state
```

Refused, 5 of them:

- `analytics.encounter_daily` what is the smallest group left after masking? cannot measure residual risk on analytics.encounter_daily while analytics.encounter_daily.day is unresolved. Closes when the columns it names are decided in the review queue.
- `raw.claim` what is the smallest group left after masking? cannot measure residual risk on raw.claim while raw.claim.submitted_on is unresolved. Closes when the columns it names are decided in the review queue.
- `raw.device_reading` what is the smallest group left after masking? cannot measure residual risk on raw.device_reading while raw.device_reading.taken_at is unresolved. Closes when the columns it names are decided in the review queue.
- `raw.encounter` what is the smallest group left after masking? cannot measure residual risk on raw.encounter while raw.encounter.admitted_at is unresolved. Closes when the columns it names are decided in the review queue.
- `raw.patient` what is the smallest group left after masking? cannot measure residual risk on raw.patient while raw.patient.city is unresolved. Closes when the columns it names are decided in the review queue.

## Who could have read it, and who did

Permission and use, kept apart. The could column follows the value through lineage, so it counts users a table level grant review misses.

```
analytics.encounter_daily.day                  6 could read,   6 did,   0 never used it
analytics.encounter_daily.postal_code          6 could read,   6 did,   0 never used it
raw.claim.member_number                        3 could read,   3 did,   0 never used it
raw.claim.submitted_on                         3 could read,   3 did,   0 never used it
raw.device_reading.taken_at                    3 could read,   3 did,   0 never used it
raw.encounter.admitted_at                      6 could read,   6 did,   0 never used it
raw.patient.birth_date                         1 could read,   1 did,   0 never used it
raw.patient.postal_code                        6 could read,   6 did,   0 never used it
raw.patient.sex                                1 could read,   1 did,   0 never used it
```

Refused, 16 of them:

- `raw.claim.payer_name` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.device_reading.device_serial` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.device_reading.source_ip` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.encounter.attending_npi` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.encounter.clinical_note` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.encounter.discharged_at` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.encounter.primary_diagnosis` did anybody read this column? nobody named it and 3 users ran a star over a table holding it, so the answer is between 0 and 3. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.city` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.created_at` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.email` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.first_name` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.last_name` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.mrn` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.phone` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.ssn` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.
- `raw.patient.street_address` did anybody read this column? nobody named it and 1 user ran a star over a table holding it, so the answer is between 0 and 1. Closes when the query log records the columns a star expanded to, or the catalog keeps enough history to expand it afterwards.

## Against the published clause list

HIPAA Safe Harbor is the answer key. It has another author, which is the only reason a recall denominator here means anything.

```
18 published Safe Harbor clauses, 16 of them reach a category here
10 of 20 in scope columns found by the floor scan
the rules behind that numerator were written with the answer key open
```

Refused, 2 of them:

- `Safe Harbor clause E` is this clause satisfied? Fax numbers. Folded into the phone category. A fax number and a telephone number have the same shape and this taxonomy cannot separate them, so a separate category would be a label nothing could ever assign. Nothing closes this.
- `Safe Harbor clause R` is this clause satisfied? Any other unique identifying number, characteristic or code. Not a category. It is a catch all whose membership depends on the data rather than on the column, so nothing in a metadata driven taxonomy can represent it. Named here so the gap is on the record. Nothing closes this.
